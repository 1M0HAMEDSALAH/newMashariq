# دليل بنية نظام مشارق (Angular 18 Enterprise Architecture)

أهلاً بك يا بطل في عالم Angular! بما إنك بتتعلم Angular، فالهيكلية اللي عملناها دي هي "العيار الثقيل" (Enterprise-Grade) اللي بتستخدم في الشركات الكبيرة. خليني أشرح لك الأجزاء الرئيسية ببساطة:

---

## 1. هيكلية الفولدرات (Folder Structure)
قسمنا الأبلكيشن لـ 4 مناطق رئيسية عشان الكود ميتعبش منك لما المشروع يكبر:

*   **`core/` (القلب)**: هنا بنحط الحاجات اللي بتشتغل مرة واحدة في السيستم كله.
    *   `services/`: الخدمات اللي بتجيب بيانات (Auth, Sidebar).
    *   `interceptors/`: "المراقب" اللي بيشوف أي API رايح أو جاي ويضيف عليه التوكين أو يعالج الأخطاء.
    *   `models/`: التعريفات (Interfaces) عشان نعرف شكل البيانات (TypeScript types).
*   **`shared/` (المشتركات)**: لو عملت زرار أو Spinner وعاوز تستخدمه في 10 صفحات، بتحطه هنا.
*   **`features/` (الأنظمة)**: كل نظام مستقل بذاته (Login, Dashboard). ده بيخليك لما تضيف ميزة "شؤون الموظفين" تحطها في فولدر مستقل تماماً.
*   **`layout/` (الهيكل)**: ده "البرواز" بتاع السيستم (المنيو الجانبي والهيدر).

---

## 2. المكونات (Standalone Components)
في Angular 18، كل صفحة بنسميها `Component`. المكون بيتكون من 3 حاجات:
1.  **TypeScript (`.ts`)**: ده "المخ" اللي فيه الكود والمنطق.
2.  **HTML (`.html`)**: ده "الجسم" أو الهيكل.
3.  **CSS (`.css`)**: ده "اللبس" أو التنسيق.

**ملاحظة**: إحنا شغالين بنظام `standalone: true` وده معناه إن المكون "حر" مش محتاج `Module` معقد عشان يشتغل.

---

## 3. إدارة الحالة (Signals)
بدل ما نستخدم متغيرات عادية، بنستخدم **Signals**. دي أحدث ميزة في Angular:
*   هي عبارة عن "مخزن" للقيمة، وأي مكان في الـ HTML بيستخدمها "بيحس" فوراً لما القيمة تتغير ويحدث نفسه من غير ما نطلب منه.
*   مثال: `isCollapsed = signal(false)`.

---

## 4. نظام التنقل (Routing & Shell)
إحنا استخدمنا نظام **Layout Shell**:
*   الـ `LayoutComponent` هو اللي شايل المنيو والهيدر.
*   جوا الـ HTML بتاعه فيه وسم اسمه `<router-outlet>`.
*   الـ Routing متظبط إن أي صفحة (زي Home) تدخل جوه الـ Outlet ده، فبالتالي المنيو بيفضل ثابت والصفحات بس هي اللي بتتغير.

---

## 5. الخدمات (Services) و الـ Injection
الـ Services هي اللي بتتعامل مع الـ API. إحنا بنستخدم ميزة الـ `inject()`:
*   بدل ما نعرف الخدمة في الـ Constructor، بنقول `private auth = inject(AuthService)`.
*   ده بيخلي الخدمة متاحة في أي مكون بسهولة.

---

## 6. البرمجة التفاعلية (RxJS)
لما بننادي الـ API، البيانات مش بترجع فوراً، بترجع كـ **Observable**:
*   تخيله كأنه "ماسورة" بيانات هتنزل فيها القيمة لما السيرفر يرد.
*   بنستخدم `.subscribe()` عشان "نشترك" في الماسورة دي ونعرف الرد.
*   بنستخدم `.pipe()` عشان "نفلتر" أو نعدل البيانات وهي ماشية (زي ما عملنا في الـ Interceptor).

---

## 7. الـ Interceptor (الشرطي)
الـ `apiInterceptor` هو أهم قطعة احترافية عملناها:
*   بيضيف `Authorization: Bearer` لكل Request رايح للسيرفر أوتوماتيك.
*   بيشغل الـ `LoadingService` عشان يظهر الـ Spinner أول ما الطلب يبدأ، ويخفيه لما يخلص.
*   بيعمل `Retry` لو النت فصل لحظة.

---

---

## 8. شرح تفصيلي لـ `LayoutComponent` (The Shell)
هذا المكون هو "البرواز" أو "الحاوية" (Container) الأساسية للنظام:

*   **المكونات الفرعية**: يقوم باستدعاء `<app-sidebar>` لعرض القائمة و `<app-header>` لعرض الشريط العلوي.
*   **`<router-outlet>`**: هذا هو المكان السحري. أي صفحة داخلية (مثل Dashboard) يتم حقنها في هذا المكان. فبالتالي تظل القائمة ثابتة والصفحات فقط هي التي تتغير.
*   **التحكم في القائمة**: يستخدم الـ `SidebarService` لمراقبة حالة القائمة (مفتوحة أم مغلقة). إذا كانت مغلقة، يضيف كلاس `sidebar-collapsed` أوتوماتيكياً لتوسيع مساحة العمل.
*   **تحميل البيانات**: بمجرد تشغيل النظام (`ngOnInit`) يقوم بطلب بيانات المنيو من السيرفر لضمان سرعة الاستجابة.

---

### نصيحة للتعلم:
ركز على ملف `api.interceptor.ts` وملف `layout.component.ts`. دول فيهم "زتونة" الـ Angular الاحترافي. 

لو احتجت أشرح لك أي سطر كود بالتفصيل، أنا موجود!
